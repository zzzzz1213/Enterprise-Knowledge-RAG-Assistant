# ENTERPRISE_ROADMAP

本文件记录「企业知识库智能助手」后续高级优化路线。当前 GitHub 上传版本定位为初版，重点验证知识库管理、向量化、RAG 问答、来源引用和本地演示模型链路。后续目标是把项目从“知识库聊天 Demo”升级为“企业级 Agentic RAG 知识研究助手”。

## 总体定位

后续项目可以升级为：

> 一个支持多步知识检索、证据引用和模型路由的企业级 Agentic RAG 系统。

核心变化：

- 从“用户直接和知识库聊天”升级为“Agent 调用知识库工具完成任务”。
- 从“单次检索回答”升级为“DeepSearch / DeepResearch 多步研究流程”。
- 从“默认本地小模型”升级为“模型路由，根据任务选择本地、云端或企业内网模型”。
- 从“可演示功能”升级为“权限、审计、部署、安全、监控更完整的企业工程”。

## 方向一：Agent 工具型 RAG

当前模式：

```text
用户问题 -> 知识库检索 -> 模型回答
```

目标模式：

```text
用户任务
-> Agent 理解任务
-> 判断是否需要查知识库
-> 调用知识库检索工具
-> 必要时多轮检索
-> 汇总证据
-> 生成有来源支撑的答案或报告
```

建议新增工具：

- `KnowledgeSearchTool`：根据问题检索指定知识库。
- `DocumentQuoteTool`：返回可引用的原文片段。
- `AnswerVerifierTool`：检查回答中的结论是否有来源支撑。
- `ReportWriterTool`：把检索结果整理成结构化报告。

适合场景：

- 企业制度问答
- 合同条款分析
- 内部流程查询
- 多文档归纳总结
- 项目资料研究

## 方向二：DeepSearch / DeepResearch 工作流

目标是让 Agent 不只检索一次，而是执行多步研究。

推荐流程：

```text
理解问题
-> 生成子问题
-> 分别检索
-> 汇总证据
-> 发现缺口
-> 二次检索
-> 输出结构化结论
```

最终回答结构建议：

- 结论
- 依据
- 引用来源
- 不确定点
- 建议补充资料

可优先实现的最小版本：

1. 用户输入一个复杂问题。
2. Agent 将问题拆成 2 到 4 个子问题。
3. 每个子问题调用知识库检索。
4. 合并来源片段并去重。
5. 输出带来源的结构化回答。

## 方向三：Model Routing 模型路由

当前本地 `qwen2:0.5b` 适合开发和演示，但企业正式使用时需要更灵活的模型策略。

建议增加模型路由层：

```text
简单问题 -> 本地小模型
复杂推理 -> 云端强模型
长文总结 -> 长上下文模型
敏感数据 -> 本地模型或企业内网模型
检索排序 -> embedding / rerank 模型
```

路由判断维度：

- 问题复杂度
- 是否需要知识库
- 是否需要多步推理
- 是否包含敏感信息
- 上下文长度
- 用户选择速度优先或质量优先
- 成本预算

企业部署建议：

- 本地开发 / 课程演示：Ollama + `qwen2:0.5b`
- 中小团队试用：DeepSeek、OpenAI、通义千问、智谱、腾讯混元等云端 API
- 高安全企业场景：企业内网私有化模型服务

## 方向四：RAG 质量优化

针对当前 RAG 常见痛点，后续可逐步增强：

- 文档结构化分块：按标题、条款、段落切分，避免一个 chunk 混入太多制度。
- Query Rewrite：先把用户口语问题改写成更适合检索的问题。
- Multi-query Retrieval：为同一问题生成多个检索查询。
- Hybrid Search 动态调权：根据问题类型调整 BM25 与向量检索权重。
- Rerank：用重排模型筛掉低相关 chunk。
- Context Compression：只保留和问题直接相关的句子。
- Citation Grounding：每个结论必须绑定来源。
- No-answer Detection：没有依据时明确拒答。
- Long Context 防丢失：先分组检索与总结，再综合回答。

## 方向五：企业级产品化

从企业用户角度，后续应补齐：

- 权限管理：不同用户只能访问授权知识库。
- 团队空间：企业知识库、部门知识库、个人知识库。
- 审计日志：记录谁问了什么、引用了哪些文档。
- 文档版本管理：制度更新后可追踪历史版本。
- 知识库健康状态：展示未向量化文档、低命中率文档、过期文档。
- 管理后台：模型配置、用户管理、使用量统计。
- 数据安全：密钥管理、脱敏、私有化部署说明。
- 监控告警：后端接口、模型调用、向量化任务、队列状态。

## 建议实施阶段

### 第一阶段：RAG 工具化

目标：知识库不再只是聊天开关，而是 Agent 可调用的工具。

优先任务：

- 抽象 `KnowledgeSearchTool`
- 抽象来源引用结构
- 聊天接口支持 Agent 调用工具
- 保留当前普通 RAG 问答作为兼容模式

### 第二阶段：DeepResearch 工作流

目标：支持多步检索、证据汇总、结构化报告。

优先任务：

- 子问题拆解
- 多轮检索
- 来源去重
- 结构化答案模板

### 第三阶段：Model Routing

目标：根据任务复杂度、敏感性、上下文长度选择模型。

优先任务：

- 建立模型配置表
- 设计模型路由规则
- 区分本地模型、云端模型和企业内网模型
- 前端展示当前任务使用的模型与原因

### 第四阶段：企业级工程化

目标：补齐权限、审计、团队空间、部署、安全、监控。

优先任务：

- 团队 / 部门空间
- 知识库权限控制
- 操作与问答审计
- 配置中心和密钥管理
- 部署说明与监控面板

## 下一步推荐

下一轮最适合先做：

```text
Agentic RAG 第一阶段：把知识库检索封装成 Agent Tool
```

原因：

- 改动目标清晰。
- 能直接提升项目高级感。
- 不需要立刻替换现有 RAG 链路。
- 可以保留当前聊天功能，同时增加“Agent 研究模式”。

