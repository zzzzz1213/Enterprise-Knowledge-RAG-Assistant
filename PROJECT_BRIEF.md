# PROJECT_BRIEF

项目名称：企业知识库智能助手

定位：
- 面向企业内部知识沉淀、文件管理、权限协作与智能问答
- 以登录注册、知识库、文件管理、Agent 助手为核心
- 第一轮优先完成品牌替换和企业化入口收敛
- 已修复知识库页搜索清空按钮的 Vue 模板事件解析错误
- 本地默认模型统一为 `qwen2:0.5b`，避免继续请求未安装的 `llama2`
- 已将聊天接口与知识库问答桥接，开启 RAG 模式时会按所选知识库执行检索
- 企业版登录页已清理 QQ / 微信入口，后端默认不注册 QQ OAuth 路由
- 云端模型密钥改为通过环境变量提供，配置文件不再保存明文 API Key
- `user_profile` 已兼容新旧字段结构，注册与个人中心资料可共用同一张表
- 已补齐聊天主组件 TypeScript 类型，前端生产构建进入可验证状态
- `RagFrontend` 已可执行 `npm run build` 成功产出 `dist`
- 已修复增量向量化任务队列与文档解析兼容问题，RAG 已可基于 `qwen2:0.5b` 返回知识库引用来源
- 聊天页 RAG 模式发送链路已改为委托父组件调用 `/api/chat/send-message`，避免绕过知识库检索直连 Ollama
- 已修复聊天页 RAG 模式消息列表不同步问题，按回车发送后可立即显示用户消息与后续助手回复
- 已校正聊天页消息顺序与加载态显示，RAG 模式恢复为“旧消息在前、最新回复在底部”的正常对话流
- 已统一聊天主组件内部顺序逻辑，普通聊天与 RAG 会话均按旧到新展示，并兼容已保存的倒序历史
- 已适配 TDesign Chat 的 `column-reverse` 展示机制：业务历史保持旧到新，传入聊天组件时使用倒序展示数据
- 已修复旧版 `/api/RAG/ingest` 在中文知识库路径下生成空 `vectorstore/temp_save` 的问题，统一写入 ASCII 安全向量库目录
- 已收紧 RAG 回答提示词，禁止输出“文件名”等占位引用，并减少制度条款之间的错误拼接
- 已增加 RAG 上下文关键词裁剪，并将聊天页默认 `topK` 调整为 3，减少 `qwen2:0.5b` 混答无关制度条款
- 已增加制度类问题的抽取式回答保护层，命中明确条款时直接用原文组织答案，避免小模型继续编造罚款、调岗、绩效等不存在内容
- 智能问答页已兼容 LangChain 与 Native 两类向量库，避免知识库详情页可检索但聊天页误报未向量化
- 向量化完成状态已改为校验真实索引文件，聊天页选择知识库时会提前检测是否存在完整向量库
- 已修正旧 `/api/RAG/ingest` 写入项目根目录 `knowledge_base` 的路径分叉，统一写入 `RagBackend/knowledge_base`，并临时兼容旧路径
- 已修复向量化统计接口 500 问题，聊天页向量化状态检查可正常读取真实结果
- 用户体验已补强：知识库列表展示向量化状态，聊天页展示当前知识库/模型/检索状态，向量化完成后可直接跳转智能问答，引用来源展示更清晰
- 知识库详情页到智能问答的流程已串联：向量化完成后跳转会自动开启知识库问答并选中当前知识库
- 上传文档后会提示继续向量化，并提供“立即向量化”入口，减少漏处理文档导致的问答失败
- 知识库详情页顶部已固定展示“去智能问答”入口，点击前会检查向量化状态，避免入口只在处理完成日志中短暂出现
- 已新增 `RUNBOOK.md`，记录 VSCode 终端下的日常实验启动、自检与常见问题处理步骤
- 已重写 `readme.md` 为 GitHub 展示版，聚焦企业知识库、RAG 问答、本地模型和启动流程
- 已补强 `.gitignore`，避免上传 `.env`、本地知识库文件、向量库、数据库、构建产物和缓存
- `readme.md` 已补充初版说明：本地 `qwen2:0.5b` 是开发演示默认配置，后续企业级版本将继续完善云端模型 API 与内网模型服务接入
