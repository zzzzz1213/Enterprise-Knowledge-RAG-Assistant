# PAGE_MAP

- `readme.md` - GitHub 项目主页说明
  - 面向公开展示，说明项目简介、核心功能、技术栈、本地启动、使用流程和上传 GitHub 前检查
  - 说明当前版本为初版，本地 Ollama 模型用于开发演示，企业使用可扩展云端 API 或内网模型服务
- `ENTERPRISE_ROADMAP.md` - 企业级优化路线图
  - 记录 Agentic RAG、DeepResearch、Model Routing、RAG 质量优化和企业级产品化路线
- `.gitignore` - GitHub 上传忽略规则
  - 忽略环境变量、本地知识库文件、向量库、数据库、构建产物、日志和缓存
- `RUNBOOK.md` - VSCode 终端日常实验启动说明
  - 记录一键启动、手动四终端启动、Ollama 检查、启动后自检和常见问题
- `/LogonOrRegister` - 企业账号登录 / 注册 / 找回密码
  - 已移除 QQ / 微信第三方登录入口
- `/knowledge` - 企业知识库列表
  - 顶部支持搜索、新建、企业共享入口
  - 卡片展示向量化状态、分块数量或向量库大小，便于提前判断能否问答
- `/knowledge/knowledgeDetail/:id` - 知识库详情
  - LangChain 向量化会写入后端 ASCII 安全向量库目录，避免中文路径导致 FAISS 保存失败
  - 向量化完成提示会调用统计接口校验真实索引文件，避免目录为空时误显示成功
  - 旧版 LangChain 向量化路径已统一到 `RagBackend/knowledge_base/vectorstores`
  - 向量化成功后提供“去智能问答”入口，失败日志按普通提示与路径详情分层展示
  - “去智能问答”会携带当前 `kb_id`，聊天页自动开启知识库问答并选中该知识库
  - 页面顶部固定展示“去智能问答”按钮，点击前会检查当前知识库是否已完成向量化
  - 上传文件成功后提供“立即向量化”入口，便于按上传、处理、问答的顺序完成操作
- `/chat` - 智能问答
  - 支持普通聊天 / 知识库问答切换
  - 支持选择知识库与检索策略
  - 聊天消息、引用来源、流式响应和模型选择已补齐本地 TypeScript 类型
  - 已验证 RAG 模式可返回 `qwen2:0.5b` 回答与引用来源
  - RAG 模式发送由 `Chat.vue` 统一转发到后端，普通聊天仍保留本地流式体验
  - 已修复 RAG 模式下按回车后消息列表不刷新的前端同步问题
  - 已恢复 RAG 模式的底部追加显示与生成中提示，避免消息误入历史区顶部
  - 普通聊天、云端模型聊天与 RAG 历史读取已统一为旧消息在上、最新消息在下
  - `Chat.vue` 使用 `displayHistory` 适配 TDesign Chat 的倒序 flex 布局
  - RAG 回答提示词已要求引用真实来源名，知识库未提供规定时不编造
  - RAG 默认检索 `topK` 调整为 3，并在后端按问题关键词裁剪上下文，降低无关制度混入回答的概率
  - 制度类 RAG 问答命中明确条款时优先返回抽取式答案，减少小模型生成阶段胡编
  - 后端聊天接口会自动识别 LangChain FAISS 与 Native FAISS 向量库，和知识库详情页向量化结果保持兼容
  - 选择知识库后会提前检查向量化状态，未生成完整索引时阻止直接提问并提示处理
  - 临时兼容此前误写到项目根目录 `knowledge_base/vectorstores` 的旧向量库
  - 向量化状态检查接口已修复 500 错误，避免误提示未向量化
  - 知识库问答面板展示当前知识库、向量库状态、检索策略与模型
  - 支持从知识库详情页通过查询参数自动选中知识库，并可手动刷新当前知识库向量化状态
  - 回答引用来源支持更清晰的折叠列表、文件名、分数与片段预览
- `/agent` - Agent 任务模式
- `/files` - 文件管理
- `/history` - 历史记录
- `/service` - 模型管理
- `/settings` - 系统设置
- `/user` - 个人中心
  - 个人资料接口兼容 `name/avatar/signature` 与 `nickname/avatar_url/bio` 字段

- /chat - 智能问答
  - RAG 模式现在通过后端 KnowledgeSearchTool 调用知识库检索，接口返回继续兼容原有 reply/sources/retrieval_mode，同时补充 agent_mode/tool_calls 便于后续 Agent 编排。

- `/api/agent/deep-research` - DeepResearch 调试接口
  - 不改动聊天页现有 RAG 链路，后端独立支持复杂问题拆分、多次知识库检索、来源去重和结构化研究结果返回。

- `/api/agent/deep-research`
  - 子问题拆分已优先识别请假、迟到、报销等制度主题，避免多个制度混在同一次检索中。

- `/api/agent/deep-research`
  - 返回结果会对每个子问题答案做轻量文本清洗，使制度类结尾表达更自然。
